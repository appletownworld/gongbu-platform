# ===========================================
# 🏢 GONGBU PLATFORM - ENTERPRISE PRODUCTION
# ===========================================
# Enterprise-grade environment configuration
# Version: 1.0.0
# Date: 2025-01-21

# === ОСНОВНЫЕ НАСТРОЙКИ ===
NODE_ENV=production
COMPOSE_PROJECT_NAME=gongbu_enterprise
PORT=3000

# === ДОМЕНЫ И URL (ИЗМЕНИТЕ НА ВАШИ!) ===
DOMAIN_NAME=yourdomain.com
API_BASE_URL=https://yourdomain.com/api
WEBAPP_URL=https://yourdomain.com
TELEGRAM_WEBHOOK_URL=https://yourdomain.com/webhook
WEBSOCKET_URL=wss://yourdomain.com/ws

# === БАЗА ДАННЫХ (ENTERPRISE GRADE) ===
POSTGRES_DB=gongbu_enterprise
POSTGRES_USER=gongbu_enterprise_user
POSTGRES_PASSWORD=ultra-secure-enterprise-password-64-chars-minimum
DATABASE_URL=postgresql://gongbu_enterprise_user:ultra-secure-enterprise-password-64-chars-minimum@postgres:5432/gongbu_enterprise

# === REDIS (CLUSTER READY) ===
REDIS_PASSWORD=enterprise-redis-password-32-chars-minimum
REDIS_URL=redis://:enterprise-redis-password-32-chars-minimum@redis:6379
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# === JWT (ENTERPRISE SECURITY) ===
JWT_SECRET=enterprise-grade-64-char-secret-key-with-randomness-and-complexity
JWT_EXPIRES_IN=15m
REFRESH_TOKEN_EXPIRES_IN=7d
JWT_ISSUER=gongbu-enterprise
JWT_AUDIENCE=gongbu-platform

# === ENCRYPTION (ENTERPRISE GRADE) ===
ENCRYPTION_KEY=32-char-enterprise-encryption-key
WEBHOOK_SIGNATURE_SECRET=32-char-webhook-secret-key
API_KEY_SECRET=enterprise-api-key-secret-32-chars

# === TELEGRAM BOT (PRODUCTION) ===
TELEGRAM_BOT_TOKEN=your_production_bot_token_from_botfather
TELEGRAM_BOT_USERNAME=your_production_bot_username
TELEGRAM_WEBHOOK_SECRET=telegram-webhook-secret-32-chars
TELEGRAM_PAYMENT_TOKEN=your_telegram_payment_token
ADMIN_USER_IDS=123456789,987654321

# === ПЛАТЕЖИ (ENTERPRISE) ===
# Stripe
STRIPE_SECRET_KEY=sk_live_enterprise_stripe_secret_key
STRIPE_PUBLISHABLE_KEY=pk_live_enterprise_stripe_publishable_key
STRIPE_WEBHOOK_SECRET=whsec_enterprise_webhook_secret
STRIPE_CURRENCY=USD

# YooKassa
YOOKASSA_SECRET_KEY=live_enterprise_yookassa_secret_key
YOOKASSA_SHOP_ID=your_yookassa_shop_id
YOOKASSA_CURRENCY=RUB

# === EMAIL (ENTERPRISE) ===
# SendGrid
SENDGRID_API_KEY=SG.enterprise_sendgrid_api_key
EMAIL_FROM=noreply@yourdomain.com
EMAIL_FROM_NAME=Gongbu Platform

# SMTP (альтернатива)
SMTP_HOST=smtp.enterprise.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=enterprise@yourdomain.com
SMTP_PASSWORD=enterprise_smtp_password

# === МОНИТОРИНГ (ENTERPRISE) ===
# Sentry
SENTRY_DSN=https://enterprise_sentry_dsn@sentry.io/project_id
SENTRY_ENVIRONMENT=production

# Prometheus
PROMETHEUS_ENABLED=true
PROMETHEUS_PORT=9090

# Grafana
GRAFANA_ADMIN_PASSWORD=enterprise_grafana_password_32_chars

# === БЕЗОПАСНОСТЬ (ENTERPRISE) ===
# Rate Limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_SKIP_SUCCESSFUL_REQUESTS=false

# CORS
CORS_ORIGIN=https://yourdomain.com
CORS_CREDENTIALS=true

# Security Headers
SECURITY_HEADERS_ENABLED=true
HELMET_ENABLED=true
CONTENT_SECURITY_POLICY=true

# === ФАЙЛОВОЕ ХРАНЕНИЕ (ENTERPRISE) ===
# AWS S3
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
AWS_REGION=us-east-1
AWS_S3_BUCKET=gongbu-enterprise-files
AWS_S3_ENDPOINT=https://s3.amazonaws.com

# Local Storage (fallback)
FILE_STORAGE_TYPE=s3
FILE_STORAGE_PATH=/app/uploads
MAX_FILE_SIZE=52428800
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/gif,video/mp4,application/pdf

# === ПОИСК (ENTERPRISE) ===
ELASTICSEARCH_URL=http://elasticsearch:9200
ELASTICSEARCH_INDEX_PREFIX=gongbu_enterprise
ELASTICSEARCH_USERNAME=elastic
ELASTICSEARCH_PASSWORD=enterprise_elasticsearch_password

# === АНАЛИТИКА (ENTERPRISE) ===
# Google Analytics
GOOGLE_ANALYTICS_MEASUREMENT_ID=G-XXXXXXXXXX
GOOGLE_ANALYTICS_API_SECRET=your_ga_api_secret

# Mixpanel
MIXPANEL_TOKEN=your_mixpanel_token
MIXPANEL_SECRET=your_mixpanel_secret

# === УВЕДОМЛЕНИЯ (ENTERPRISE) ===
# Push Notifications (Firebase)
FIREBASE_PROJECT_ID=gongbu-enterprise
FIREBASE_PRIVATE_KEY=your_firebase_private_key
FIREBASE_CLIENT_EMAIL=firebase-adminsdk@your-project.iam.gserviceaccount.com

# SMS (Twilio)
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=+1234567890

# === ЛОГИРОВАНИЕ (ENTERPRISE) ===
LOG_LEVEL=info
LOG_FORMAT=json
LOG_FILE_ENABLED=true
LOG_FILE_PATH=/app/logs
LOG_ROTATION_ENABLED=true
LOG_RETENTION_DAYS=30

# === ПРОИЗВОДИТЕЛЬНОСТЬ (ENTERPRISE) ===
# Connection Pooling
DB_POOL_MIN=2
DB_POOL_MAX=20
DB_POOL_IDLE_TIMEOUT=30000

# Caching
CACHE_TTL=3600
CACHE_MAX_SIZE=1000

# === SSL/TLS (ENTERPRISE) ===
SSL_ENABLED=true
SSL_CERT_PATH=/etc/letsencrypt/live/yourdomain.com/fullchain.pem
SSL_KEY_PATH=/etc/letsencrypt/live/yourdomain.com/privkey.pem
SSL_REDIRECT_HTTP_TO_HTTPS=true

# === BACKUP (ENTERPRISE) ===
BACKUP_ENABLED=true
BACKUP_SCHEDULE=0 0 */6 * * *
BACKUP_RETENTION_DAYS=30
BACKUP_S3_BUCKET=gongbu-enterprise-backups

# === МАСШТАБИРОВАНИЕ (ENTERPRISE) ===
# Load Balancer
LOAD_BALANCER_ENABLED=true
LOAD_BALANCER_ALGORITHM=round_robin

# Auto Scaling
AUTO_SCALE_ENABLED=true
AUTO_SCALE_MIN_INSTANCES=2
AUTO_SCALE_MAX_INSTANCES=10
AUTO_SCALE_CPU_THRESHOLD=70

# === РЕЗЕРВНОЕ КОПИРОВАНИЕ (ENTERPRISE) ===
# Database Replication
DB_REPLICATION_ENABLED=true
DB_MASTER_URL=postgresql://master_user:master_password@master-db:5432/gongbu_enterprise
DB_SLAVE_URL=postgresql://slave_user:slave_password@slave-db:5432/gongbu_enterprise

# Redis Cluster
REDIS_CLUSTER_ENABLED=true
REDIS_CLUSTER_NODES=redis-1:6379,redis-2:6379,redis-3:6379

# === CDN (ENTERPRISE) ===
CDN_ENABLED=true
CDN_PROVIDER=cloudflare
CDN_DOMAIN=cdn.yourdomain.com
CDN_CACHE_TTL=86400

# === МОНИТОРИНГ ПРОИЗВОДИТЕЛЬНОСТИ (ENTERPRISE) ===
# APM
APM_ENABLED=true
APM_PROVIDER=newrelic
NEW_RELIC_LICENSE_KEY=your_newrelic_license_key
NEW_RELIC_APP_NAME=Gongbu-Enterprise

# === АУДИТ И СООТВЕТСТВИЕ (ENTERPRISE) ===
AUDIT_LOG_ENABLED=true
AUDIT_LOG_LEVEL=info
GDPR_COMPLIANCE_ENABLED=true
DATA_RETENTION_DAYS=2555
USER_CONSENT_REQUIRED=true

# === ТЕСТИРОВАНИЕ (ENTERPRISE) ===
TEST_MODE=false
MOCK_EXTERNAL_SERVICES=false
PERFORMANCE_TESTING_ENABLED=false

# === РАЗРАБОТКА (ENTERPRISE) ===
DEBUG_MODE=false
VERBOSE_LOGGING=false
PROFILING_ENABLED=false

# === ИНТЕГРАЦИИ (ENTERPRISE) ===
# Webhooks
WEBHOOK_ENABLED=true
WEBHOOK_TIMEOUT=30000
WEBHOOK_RETRY_ATTEMPTS=3

# API Keys
API_KEY_VALIDATION_ENABLED=true
API_KEY_RATE_LIMIT=1000

# === БЕЗОПАСНОСТЬ ДАННЫХ (ENTERPRISE) ===
# Data Encryption
DATA_ENCRYPTION_ENABLED=true
ENCRYPTION_ALGORITHM=aes-256-gcm

# PII Protection
PII_MASKING_ENABLED=true
PII_FIELDS=email,phone,address

# === МОНИТОРИНГ БЕЗОПАСНОСТИ (ENTERPRISE) ===
# Security Scanning
SECURITY_SCANNING_ENABLED=true
VULNERABILITY_SCANNING_ENABLED=true
DEPENDENCY_SCANNING_ENABLED=true

# === АВАРИЙНОЕ ВОССТАНОВЛЕНИЕ (ENTERPRISE) ===
# Disaster Recovery
DR_ENABLED=true
DR_RTO_MINUTES=240
DR_RPO_MINUTES=60
DR_BACKUP_LOCATION=multi-region

# === МЕТРИКИ И АЛЕРТЫ (ENTERPRISE) ===
# Alerting
ALERTING_ENABLED=true
ALERT_EMAIL=alerts@yourdomain.com
ALERT_SLACK_WEBHOOK=https://hooks.slack.com/services/your/slack/webhook

# Health Checks
HEALTH_CHECK_INTERVAL=30
HEALTH_CHECK_TIMEOUT=10
HEALTH_CHECK_RETRIES=3

# === ФИНАЛЬНЫЕ НАСТРОЙКИ (ENTERPRISE) ===
# Feature Flags
FEATURE_FLAGS_ENABLED=true
FEATURE_FLAGS_PROVIDER=launchdarkly
LAUNCHDARKLY_SDK_KEY=your_launchdarkly_sdk_key

# A/B Testing
AB_TESTING_ENABLED=true
AB_TESTING_PROVIDER=optimizely
OPTIMIZELY_SDK_KEY=your_optimizely_sdk_key

# === ПРИМЕЧАНИЯ ===
# 1. Замените все значения "your_*" на реальные
# 2. Используйте сильные пароли (минимум 32 символа)
# 3. Регулярно обновляйте секретные ключи
# 4. Настройте мониторинг всех сервисов
# 5. Проведите тестирование безопасности
# 6. Настройте автоматические бэкапы
# 7. Документируйте все изменения
