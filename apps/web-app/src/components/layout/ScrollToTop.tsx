import { useScrollToTop } from '@/hooks/useScrollToTop';

/**
 * 📜 Компонент автоматической прокрутки наверх при переходах между страницами
 * 
 * Решает проблему UX: когда пользователь переходит на новую страницу,
 * он должен видеть её содержимое с самого начала, а не оставаться
 * в том же месте прокрутки, что и на предыдущей странице.
 * 
 * Особенности:
 * - Плавная прокрутка для лучшего UX
 * - Работает для всех переходов по маршрутам
 * - Не мешает внутристраничной навигации (якоря)
 */
const ScrollToTop: React.FC = () => {
  // Используем кастомный хук для прокрутки наверх
  useScrollToTop(
    true, // smooth - плавная прокрутка
    100,  // delay - небольшая задержка для лучшей анимации
    []    // excludePaths - пути исключений (пока пусто)
  );

  // Компонент не рендерит ничего, только выполняет side effect
  return null;
};

export default ScrollToTop;
